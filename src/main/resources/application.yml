spring:
  servlet:
    multipart:
      max-file-size: 256MB
      max-request-size: 256MB
  logging:
    level:
      root: DEBUG
  datasource:
    url: jdbc:postgresql://localhost:5432/refcloudapi
    username: refcloudapi
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
